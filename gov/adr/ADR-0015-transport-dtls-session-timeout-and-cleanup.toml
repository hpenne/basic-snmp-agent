[govctl]
schema = 1
id = "ADR-0015"
title = "transport: DTLS session timeout and cleanup"
status = "proposed"
date = "2026-03-04"
refs = [
    "RFC-0001",
    "ADR-0004",
]

[content]
context = """
ADR-0004 uses a HashMap<SocketAddr, Dtls> for session routing but defers cleanup policy.
Without a timeout mechanism, stale sessions from disconnected or NAT-rebound peers accumulate indefinitely.
"""
decision = "TBD"
consequences = "TBD"
