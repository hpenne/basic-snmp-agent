[[gap]]
req     = "REQ-0001"
missing = ["behave_test"]
rationale = "Structural constraint: the crate being a library (not a binary) is enforced by the Cargo manifest and verified at compile time. A Behave system test cannot introspect the crate type."

[[gap]]
req     = "REQ-0002"
missing = ["behave_test"]
rationale = "Architectural constraint: every Behave scenario implicitly relies on the public Agent type, but tagging every scenario with REQ-0002 would dilute traceability without adding information. The constraint is verified by compilation and unit tests."

[[gap]]
req     = "REQ-0003"
missing = ["behave_test"]
rationale = "Structural constraint: the absence of global mutable state is a design property enforced by the Rust type system (no static mut, no lazy_static). A Behave system test cannot observe the absence of global state."

[[gap]]
req     = "REQ-0048"
missing = ["behave_test"]
rationale = "Architectural constraint: the event loop running on a dedicated OS thread is an implementation detail not observable at the system-test boundary. Verified by unit tests that spawn and join the event loop thread."

[[gap]]
req     = "REQ-0049"
missing = ["behave_test"]
rationale = "Build constraint: the absence of an async runtime dependency is enforced by the Cargo manifest (no tokio/async-std). A Behave system test cannot inspect build dependencies."

[[gap]]
req     = "REQ-0053"
missing = ["behave_test"]
rationale = "Architectural constraint: the absence of blocking operations on the event loop thread is a design property verified by code review and the non-blocking I/O patterns used throughout. A Behave test cannot directly observe whether a call blocks."

[[gap]]
req     = "REQ-0054"
missing = ["behave_test"]
rationale = "Architectural constraint: keeping long-running work off the event loop thread is a design property enforced by the synchronous event loop architecture. Not directly observable at the system-test boundary."

[[gap]]
req     = "REQ-0064"
missing = ["behave_test"]
rationale = "Type system constraint: Agent being Send+Sync, making set() safe to call from any thread, is enforced by the Rust type checker at compile time. A Behave system test cannot exercise thread-safety properties."

[[gap]]
req     = "REQ-0065"
missing = ["behave_test"]
rationale = "Type signature constraint: set() taking &self rather than &mut self is enforced by the Rust type system and verified by unit tests that call it on a non-mut binding. A Behave system test cannot introspect method signatures."

[[gap]]
req     = "REQ-0067"
missing = ["behave_test"]
rationale = "Negative/absence constraint: the non-existence of a delete/remove API on Agent or Store is enforced by the type system at compile time. No runtime test can verify the absence of a method."

[[gap]]
req     = "REQ-0043"
missing = ["behave_test"]
rationale = "The empty-destinations error is returned before any PDU is sent. A system-level Behave test cannot meaningfully distinguish this early-return case from other failures, and the requirement is fully covered by a unit test."

[[gap]]
req     = "REQ-0046"
missing = ["rust_test"]
rationale = "Absence/negative constraint: the non-existence of a send_inform API is enforced by the type system at compile time. No runtime test can verify the absence of a method; the constraint is satisfied by the public API having no such function."

[[gap]]
req     = "REQ-0028"
missing = ["behave_test"]
rationale = "Negative max-repetitions values are rejected at BER decode level (rasn cannot store a negative INTEGER in a u32 field). Net-snmp CLI tools cannot send negative max-repetitions, so a Behave system test is not feasible; the behaviour is covered by a unit test."

[[gap]]
req     = "REQ-0059"
missing = ["code", "rust_test", "behave_test"]
rationale = "REQ-0059 is a SHOULD: the agent SHOULD respond with a Report PDU incrementing snmpUnknownContexts. The current implementation silently discards requests with a non-empty context name (the required minimum). The Report PDU enhancement is deferred and will be tracked separately."

[[gap]]
req     = "REQ-0076"
missing = ["behave_test"]
rationale = "Library API constraint: the single-user-at-construction invariant is enforced by the Rust type system (AgentBuilder accepts exactly one UsmUser). A Behave system test cannot introspect API shape."

[[gap]]
req     = "REQ-0077"
missing = ["behave_test"]
rationale = "Internal immutability constraint: the security level being fixed at construction time is enforced by the Rust type system (no setter method). Not observable from the protocol level."

[[gap]]
req     = "REQ-0081"
missing = ["behave_test"]
rationale = "Configuration API constraint: rejection of plaintext passwords is enforced at construction time before any network interaction. A Behave system test cannot exercise the builder API directly."

[[gap]]
req     = "REQ-0082"
missing = ["behave_test"]
rationale = "Library API: localised key derivation is a pure function tested by unit tests. Not observable from protocol-level system tests."

[[gap]]
req     = "REQ-0083"
missing = ["behave_test"]
rationale = "Configuration-time key length validation is enforced at construction before any network interaction. A Behave system test cannot exercise builder-level validation."

[[gap]]
req     = "REQ-0084"
missing = ["behave_test"]
rationale = "Internal key derivation algorithm (priv-key-from-auth) is an implementation detail not directly observable from the protocol. Verified by unit tests."

[[gap]]
req     = "REQ-0085"
missing = ["behave_test"]
rationale = "Memory zeroisation via write_volatile is a runtime safety property not observable from the network protocol. Verified by unit tests inspecting memory contents."

[[gap]]
req     = "REQ-0087"
missing = ["behave_test"]
rationale = "HMAC-SHA-512 is testable in principle but no SHA-512 test infrastructure (snmptrapd createUser with SHA-512, net-snmp CLI with SHA-512) exists in the current system test environment."

[[gap]]
req     = "REQ-0089"
missing = ["behave_test"]
rationale = "AES-256-CFB is testable in principle but no AES-256 test infrastructure (snmptrapd createUser with AES-256, net-snmp CLI with AES-256) exists in the current system test environment."

[[gap]]
req     = "REQ-0090"
missing = ["rust_test", "behave_test"]
rationale = "Compile-time type safety constraint: the type system prevents constructing invalid auth/priv combinations. Tested by successful compilation, not runtime assertions. Neither unit tests nor Behave tests can verify compile-time type constraints."

[[gap]]
req     = "REQ-0092"
missing = ["behave_test"]
rationale = "Compile-time type constraint: key-length matching between auth protocol and key type is enforced by the Rust type system. Not observable from protocol-level tests."

[[gap]]
req     = "REQ-0095"
missing = ["behave_test"]
rationale = "Internal trait implementation: EngineBootsStore being a trait with a file-backed implementation is an architectural choice verified by unit tests. Not observable from the protocol level."

[[gap]]
req     = "REQ-0096"
missing = ["rust_test", "behave_test"]
rationale = "File I/O atomicity implementation detail: atomic write-rename for the boots file is an implementation safety measure. Not directly testable via assertions (would require fault injection). Verified by code review of the atomic write-rename implementation."

[[gap]]
req     = "REQ-0097"
missing = ["behave_test"]
rationale = "Boots counter overflow at 2^31-1 is impractical to test at the system level (would require simulating 2^31-1 reboots). Verified by unit tests."

[[gap]]
req     = "REQ-0102"
missing = ["behave_test"]
rationale = "Internal RFC 3414 §3.2 processing order (user-name → level → HMAC → time → decrypt) is an implementation detail not directly observable from the protocol. The correct order is enforced by code structure and verified by unit tests."

[[gap]]
req     = "REQ-0108"
missing = ["behave_test"]
rationale = "Intermediate key material zeroisation is a runtime safety property not observable from the network protocol. Verified by unit tests inspecting memory contents."

[[gap]]
req     = "REQ-0109"
missing = ["behave_test"]
rationale = "IV construction (boots||time||salt) is an internal cryptographic detail not observable from the protocol. Verified by unit tests that decrypt and round-trip encrypted PDUs."

[[gap]]
req     = "REQ-0110"
missing = ["behave_test"]
rationale = "Salt uniqueness (monotonic counter) is an internal cryptographic property. A system test cannot reliably inspect the raw salt bytes. Verified by unit tests comparing consecutive salts."

[[gap]]
req     = "REQ-0111"
missing = ["behave_test"]
rationale = "Auth params zeroing before MAC computation is an internal step not observable from the protocol. Verified by unit tests that inspect the zeroed buffer."

[[gap]]
req     = "REQ-0112"
missing = ["behave_test"]
rationale = "MAC truncation length is an internal cryptographic detail. Protocol-level tests verify authentication succeeds/fails but cannot inspect the truncated MAC length directly. Verified by unit tests."

[[gap]]
req     = "REQ-0113"
missing = ["behave_test"]
rationale = "Constant-time MAC comparison is a side-channel defence not observable from protocol-level tests. Verified by unit tests confirming the subtle crate is used."

[[gap]]
req     = "REQ-0114"
missing = ["behave_test"]
rationale = "Password minimum length is a configuration-time constraint enforced before any network interaction. A Behave test cannot exercise the builder API directly. Verified by unit tests."

[[gap]]
req     = "REQ-0115"
missing = ["behave_test"]
rationale = "Security model validation requires crafting a packet with a non-USM security model field, which net-snmp CLI tools cannot produce. Verified by unit tests with hand-patched frames."

[[gap]]
req     = "REQ-0116"
missing = ["behave_test"]
rationale = "Empty user name rejection is a configuration-time constraint enforced at construction. A Behave test cannot exercise builder-level validation. Verified by unit tests."

[[gap]]
req     = "REQ-0117"
missing = ["behave_test"]
rationale = "Oversized frame rejection requires sending a frame larger than 65535 bytes over TCP, which net-snmp CLI tools cannot produce. Verified by unit tests with synthetic frames."

[[gap]]
req     = "REQ-0118"
missing = ["behave_test"]
rationale = "Negative msgID rejection requires crafting a packet with a negative BER-encoded integer, which net-snmp CLI tools cannot produce. Verified by unit tests with hand-crafted frames."

[[gap]]
req     = "REQ-0119"
missing = ["behave_test"]
rationale = "msgMaxSize < 484 rejection requires crafting a packet with an invalid msgMaxSize field, which net-snmp CLI tools cannot produce. Verified by unit tests with hand-crafted frames."

[[gap]]
req     = "REQ-0120"
missing = ["behave_test"]
rationale = "Connection limit enforcement requires opening 64+ simultaneous TCP connections and verifying rejection, which is impractical in the current Behave test infrastructure. Verified by unit tests."

[[gap]]
req     = "REQ-0121"
missing = ["behave_test"]
rationale = "Log warning on connection rejection is an internal diagnostic not observable from the protocol. Verified by unit tests that check the rejection behaviour."

[[gap]]
req     = "REQ-0122"
missing = ["behave_test"]
rationale = "Idle connection timeout requires waiting 5 minutes (or configuring a short timeout), which is impractical for system tests. Verified by unit tests with synthetic time advancement."

[[gap]]
req     = "REQ-0123"
missing = ["behave_test"]
rationale = "Timeout configurability is a builder API property not observable from the protocol. Verified by unit tests that construct agents with custom timeout configurations."

[[gap]]
req     = "REQ-0124"
missing = ["behave_test"]
rationale = "Pressure mode (shorter timeout near connection limit) requires filling connection slots and observing timeout changes, which is impractical in system tests. Verified by unit tests with synthetic time advancement."

[[gap]]
req     = "REQ-0129"
missing = ["behave_test"]
rationale = "Compile-time type constraint: the SecurityConfig enum enforces bundling of minimum security level, user credentials, and engine-boots store at construction via Rust's type system. A Behave system test cannot introspect API shape or type relationships."

[[gap]]
req     = "REQ-0131"
missing = ["behave_test"]
rationale = "User name length rejection is a configuration-time constraint enforced at construction before any network interaction. A Behave test cannot exercise builder-level validation. Verified by unit tests."

[[gap]]
req     = "REQ-0132"
missing = ["behave_test"]
rationale = "Oversized msgUserName rejection requires crafting a packet with a user name field exceeding 32 octets, which net-snmp CLI tools cannot produce. Verified by unit tests with hand-crafted BER frames."

[[gap]]
req     = "REQ-0133"
missing = ["behave_test"]
rationale = "GETBULK response size bounding requires sending a request with a specific small msgMaxSize in the HeaderData field, which net-snmp CLI tools do not expose. Verified by unit tests and a dispatch integration test with hand-crafted frames."

[[gap]]
req     = "REQ-0134"
missing = ["behave_test"]
rationale = "Row-boundary truncation of GETBULK repeating sections requires the same msgMaxSize control as REQ-0133. Verified by unit tests that assert exact varbind counts at row boundaries."
